{
  "devDependencies": {
    "@types/node": "^16.10.3",
    "gts": "^3.1.0",
    "typescript": "^4.4.3",
    "yarn-upgrade-all": "^0.5.4"
  },
  "scripts": {
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "yarn run compile",
    "pretest": "yarn run compile",
    "posttest": "yarn run lint"
  }
}
